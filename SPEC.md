# מסמך אפיון – מאמן הכתיבה המנהלית (WritingCoach)

**גרסה:** 1.0
**תאריך:** מרץ 2026

---

## 1. סקירה כללית

WritingCoach הוא כלי לימודי מבוסס בינה מלאכותית, המיועד לשיפור כישורי הכתיבה המנהלית והמקצועית של עובדים בארגונים. הכלי מנתח מסמכים שמשתמשים מגישים, מאתר פערים, ומספק שיעורי מיקרו-למידה מותאמים אישית – תוך שימוש בטקסט האמיתי של המשתמש כדוגמה לתרגול.

---

## 2. בעיה שהכלי פותר

כתיבה מנהלית נאותה היא מיומנות קריטית בשירות הציבורי ובארגונים גדולים, אך לרוב נלמדת דרך נסיון שגוי וחסר משוב מובנה. הכשרות סטנדרטיות הן כלליות ואינן מותאמות לסוג המסמך, לרמת הכותב, ולצרכים הספציפיים שלו.

WritingCoach מציע פתרון מותאם אישית: פידבק מיידי, שיעור ממוקד, ותרגול עם הערכה – כל זה בתהליך קצר ועצמאי.

---

## 3. לולאת הלמידה – 3 שלבים

### שלב 1: אבחון (Diagnostic)
- המשתמש מגיש מסמך (מכתב רשמי, סיכום ישיבה, עבודת מטה וכד')
- המערכת מעריכה את המסמך מול רובריקה מוגדרת מראש
- פלט: ציון כולל (0–100), ציון לפי קריטריון, זיהוי פערים עיקריים

### שלב 2: מיקרו-למידה (Learning)
- בהתבסס על הפערים שזוהו, המערכת מייצרת שיעור קצר ומותאם אישית
- השיעור כולל דוגמאות "לפני/אחרי" הלקוחות מהמסמך שהוגש
- הסבר ברור לכלל הכתיבה הרלוונטי

### שלב 3: הערכה ותרגול (Assessment)
- המערכת יוצרת תרגיל על בסיס קטע מהמסמך המקורי
- המשתמש כותב גרסה משופרת
- המערכת מעריכה את השיפור ומספקת ציון ומשוב מפורט
- ניתן לנסות מספר פעמים

---

## 4. סוגי מסמכים נתמכים (Recipes)

הכלי משתמש בקובצי YAML הנקראים "מתכונים" (recipes) להגדרת מאפייני כל סוג מסמך:

| סוג מסמך | קובץ | תיאור |
|---|---|---|
| מכתב רשמי | `formal-letter.yaml` | מכתבים ופניות רשמיות |
| סיכום ישיבה | `meeting-summary.yaml` | פרוטוקולים וסיכומי דיון |
| עבודת מטה | `staff-work.yaml` | מסמכי ניתוח ועמדה מקצועית |

כל מתכון מגדיר:
- **קריטריוני הערכה** עם משקלות
- **שכבת בסיס** (שפה וסגנון כללי, 40%) + **שכבה ספציפית** לסוג המסמך (60%)
- **שיעורים מובנים** עם דוגמאות

### הרחבת המתכונים
המערכת מתוכננת להרחבה – ניתן להוסיף סוגי מסמכים חדשים על ידי יצירת קובץ YAML נוסף בתיקיית `recipes/`, ללא שינוי קוד. הכלי טוען את המתכונים דינמית בזמן ריצה.

---

## 5. ממשקי משתמש

### ממשק CLI
- ממשק טרמינל אינטראקטיבי עם תמיכה מלאה בעברית
- תפריטים, ספינרים וצבעים (rich TUI)
- מתאים לסביבות ללא דפדפן / air-gap

### ממשק Web
- אפליקציית React עם עיצוב עברי (RTL, גופן Heebo)
- תקשורת עם שרת Express דרך REST API
- ניהול סשן מבוסס UUID
- מתאים לפריסה ארגונית

---

## 6. תמיכה בספקי LLM

הכלי תומך בריבוי ספקי בינה מלאכותית דרך ממשק תכנות אחיד:

| ספק | מתאים ל |
|---|---|
| Anthropic Claude (ענן) | דיוק גבוה, סביבה מחוברת לאינטרנט |
| Ollama (מקומי) | סביבות air-gap, פרטיות גבוהה |
| OpenAI-compatible (vLLM / Azure) | תשתיות ארגוניות קיימות |
| Cohere | חלופה ענן |

החלפת ספק מתבצעת דרך משתנה סביבה (`LLM_PROVIDER`) ללא שינוי קוד.

---

## 7. ארכיטקטורה טכנית

```
writingcoach/
├── src/            # לוגיקת ליבה (TypeScript)
│   ├── coach.ts    # מתזמן 3 השלבים
│   ├── engine/     # מנועי אבחון / למידה / הערכה
│   └── llm/        # לקוחות LLM (factory pattern)
├── server/         # שרת Express (REST API)
├── client/         # אפליקציית React
└── recipes/        # הגדרות סוגי מסמכים (YAML)
```

**Stack:**
- Backend: Node.js 20, TypeScript, Express 5
- Frontend: React 18, Vite, TypeScript
- Deployment: Docker, Docker Compose, Railway.app

---

## 8. אבטחה ותפעול

- **Rate limiting:** 60 בקשות לדקה לכתובת IP
- **CORS:** רשימת מקורות מורשים ניתנת להגדרה
- **Sessions:** ניהול סשן בזיכרון עם UUID
- **Health check:** נקודת קצה `/health` לניטור
- **Air-gap ready:** פריסה מלאה ללא גישה לאינטרנט עם Ollama

---

## 9. התאמה אישית של הפרומפטים

כל אחד משלושת שלבי הלמידה מונע על ידי **system prompt** ייעודי, המאוחסן בקוד (`src/engine/system-prompt-store.ts`). הפרומפטים כתובים בעברית ומכוונים לדומיין הכתיבה המנהלית.

ניתן לערוך את הפרומפטים ישירות כדי:
- לשנות טון ושפה (רשמי / לא רשמי)
- להתאים לסוג ארגון ספציפי
- לשלב מונחים מקצועיים ייחודיים
- לכוונן את רמת המשוב (מפורט / תמציתי)

כל שלב מנהל פרומפט נפרד – ניתן לכוונן כל אחד באופן עצמאי מבלי להשפיע על האחרים.

---

## 10. מגבלות ידועות

- אחסון הסשנים הוא בזיכרון בלבד (לא עמיד להפעלה מחדש)
- אין מנגנון אותנטיקציה משתמשים מובנה
- מספר המתכונים הנוכחי מוגבל לשלושה סוגי מסמכים

---

## 11. תרחישי שימוש עיקריים

1. **עובד ציבורי** שכותב מכתבים רשמיים ורוצה לשפר ניסוח
2. **מנהל** שמכין עבודות מטה ורוצה פידבק מובנה לפני הגשה
3. **מנחה הכשרה** שמשתמש בכלי כחלק מתכנית פיתוח מקצועי
4. **ארגון** שרוצה לספק כלי כתיבה עצמאי לעובדים בסביבת air-gap
